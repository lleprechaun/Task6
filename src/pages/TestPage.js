import { Navigate } from 'react-router-dom';

const TestPage = () => {

    /*----------------------------------HTML----------------------------------*/

    return (
        <div>
            <Navigate to={'/start'} />
        </div>
    )
}

export default TestPage;
